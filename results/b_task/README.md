# B Task Outputs

This directory stores B-task artifacts for:

- E3: ordinary embedding baselines
- E3.5: unified experiment registry and run commands
- E9: cross-dataset baseline evaluation reuse

Main files:

- `experiment_registry.csv`: unified structured result registry
- `run_commands.md`: reproducible command list
- `e3_embedding_baselines/`: per-run metric outputs
